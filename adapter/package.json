{
  "name": "shared-memory-adapter",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "bun build src/entry.ts --target bun --outfile dist/shared-memory.js --external fastembed --external onnxruntime-node --minify",
    "test": "bun test ./test/*.test.ts"
  }
}
